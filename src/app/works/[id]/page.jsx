export default function WorkDetail({ params }) {
    return (
          <div style={{ minHeight: "100vh", paddingTop: "80px" }}>                                                  
            {/* <h1>{params.id}</h1> */}
            メンテナンス中・・・
        </div>
    );
}